
&НаСервере
Процедура ПробитьЧекНаСервере()
	// Вставить содержимое обработчика.
	СерверККТ = ККМ_ФЗ_54СерверПереопределяемый.ПодключитьсяКККМ(Объект.КассаККМ);
	Об = РеквизитФормыВЗначение("Объект", Тип("ДокументОбъект.ксЧекККМ"));
	Рез = Об.ПровестиИНапечататьЧек(СерверККТ);
	если Рез.Результат Тогда
		Сообщить("Хорошо");
	Иначе	
		Сообщить("Ни хуя не хорошо");
	КонецЕсли;	
КонецПроцедуры

&НаКлиенте
Процедура ПробитьЧек(Команда)
	ПробитьЧекНаСервере();
КонецПроцедуры
