
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Отчет.ВариантПостроенияОтчета = 1;
	Отчет.ПериодОтчета.ДатаНачала = НачалоМесяца(ТекущаяДата());
	Отчет.ПериодОтчета.ДатаОкончания = ТекущаяДата();

КонецПроцедуры

&НаКлиенте
Процедура СформироватьОтчет(Команда)
	
	СкомпоноватьРезультат();
	
КонецПроцедуры


&НаКлиенте
Процедура СписокНомеровПриИзменении(Элемент)
	СписокНомеровЗначение = СтрРазделить(Отчет.СписокНомеров,Символы.ПС,Ложь);
	Элементы.СписокНомеров.Заголовок = "Список номеров (" + Строка(СписокНомеровЗначение.Количество()) + ")";
КонецПроцедуры

