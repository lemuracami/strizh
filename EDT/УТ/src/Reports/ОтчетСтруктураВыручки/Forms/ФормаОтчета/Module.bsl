&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ДеньСегодня = ДобавитьМесяц(ТекущаяДата(), -1);
	Период.ДатаОкончания = КонецМесяца(ДеньСегодня);
	Период.ДатаНачала = НачалоМесяца(ДеньСегодня);
	ПодставитьПараметры();
КонецПроцедуры

&НаКлиенте
Процедура ПодставитьПараметры()
	ПараметрДанных= Отчет.КомпоновщикНастроек.Настройки.ПараметрыДанных.Элементы.Найти("ДатаКон");
	ПараметрДанных.Значение = КонецДня(Период.ДатаОкончания);
	ПараметрДанных.Использование=Истина;
	
	ПараметрДанных= Отчет.КомпоновщикНастроек.Настройки.ПараметрыДанных.Элементы.Найти("ДатаНач");
	ПараметрДанных.Значение = НачалоДня(Период.ДатаНачала);
	ПараметрДанных.Использование=Истина;
КонецПроцедуры


&НаКлиенте
Процедура ПериодПриИзменении(Элемент)
	ПодставитьПараметры();
КонецПроцедуры

