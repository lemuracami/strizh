
Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	// избавимся от дублей и ошибок с ними связанных
	НаборТЗ = ЭтотОбъект.Выгрузить();	
	НаборТЗ.Свернуть("ПриходЗаказов, Заказ, МестоЗаказа, Период");	
	ЭтотОбъект.Загрузить(НаборТЗ);
	
КонецПроцедуры
