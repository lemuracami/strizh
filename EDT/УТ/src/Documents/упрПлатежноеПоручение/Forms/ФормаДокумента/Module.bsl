
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ТолькоПросмотр = Истина;
	
	Если Не Объект.Ссылка.Пустая() тогда
		Запрос = Новый Запрос;
		Запрос.УстановитьПараметр("Ссылка", Объект.Ссылка);
		Запрос.Текст =
		"ВЫБРАТЬ
		|	упрСостоянияПлатежейСрезПоследних.СостояниеПлатежа КАК СостояниеПлатежа
		|ИЗ
		|	РегистрСведений.упрСостоянияПлатежей.СрезПоследних(, Платеж = &Ссылка) КАК упрСостоянияПлатежейСрезПоследних";
		
		Выборка = Запрос.Выполнить().Выбрать();
		Выборка.Следующий();
		
		СостояниеПлатежа = Выборка.СостояниеПлатежа;
	КонецЕсли;
	
	СтрИдентификаторПП_БП = Объект.ИдентификаторПП_БП;
	
КонецПроцедуры
