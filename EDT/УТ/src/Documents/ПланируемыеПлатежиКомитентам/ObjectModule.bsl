
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	СуммаДокумента = 0;
	
	Для каждого ТекСтрока Из ПлатежиКИсполнению Цикл
		
		Если ТекСтрока.Оплачивать Тогда
			СуммаДокумента = СуммаДокумента + ТекСтрока.СуммаЗадолженности;
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры
