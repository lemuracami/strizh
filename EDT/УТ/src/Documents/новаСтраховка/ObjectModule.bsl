Процедура ПриЗаписи(Отказ)
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	Если ЗначениеЗаполнено(Основание) И Не ЭтотОбъект.Ссылка.Пустая() Тогда
		Если Основание.ПолучитьФорму("ФормаДокумента").Открыта() Тогда
			Основание.ПолучитьФорму("ФормаДокумента").Страховка = ЭтотОбъект.Ссылка;
			Основание.ПолучитьФорму("ФормаДокумента").ДокументОбъект.ДобавитьУдалитьСтрокуСтрахование();
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры