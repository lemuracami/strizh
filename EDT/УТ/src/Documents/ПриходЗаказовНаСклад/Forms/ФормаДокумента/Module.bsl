
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если ТекущийПользовательНаСервере() <> "Администратор УТЛ" И ТекущийПользовательНаСервере() <> "Марочкин Евгений" Тогда
    	ЭтаФорма.ТолькоПросмотр = Истина;
	КонецеСли;	
КонецПроцедуры

Функция ТекущийПользовательНаСервере()

	Возврат СокрЛП(ПараметрыСеанса.ТекущийПользователь.Наименование);

КонецФункции // ()

