
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Объект.Ссылка.Пустая() Тогда
		Объект.Пользователь = ПараметрыСеанса.ТекущийПользователь;		
	Иначе	
		ТолькоПросмотр = baoВызовСервера.РейсЗакрыт(Объект.Ссылка)		
	КонецЕсли;	

	Элементы.НадписьПользователь.Заголовок = "Пользователь: " +  СокрЛП(Объект.Пользователь);
		
КонецПроцедуры
