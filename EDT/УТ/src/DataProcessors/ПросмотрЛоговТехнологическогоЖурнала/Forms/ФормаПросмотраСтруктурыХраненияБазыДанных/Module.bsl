
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если НЕ Параметры.Свойство("АдресВХранилищеСтруктураХраненияБазыДанных") Тогда
		
		ТекстСообщения = НСтр("ru = 'Нет данных для просмотра структуры хранения базы данных.'");
		
		СообщениеПользователю = Новый СообщениеПользователю;
		СообщениеПользователю.Текст = ТекстСообщения;
		СообщениеПользователю.Сообщить();
		
		Отказ = Истина;
		Возврат;
		
	КонецЕсли;
	
	ОбъектОбработки = РеквизитФормыВЗначение("Объект");
	ВременноеДерево = РеквизитФормыВЗначение("ДеревоСтруктурыХраненияБазыДанных");
	
	ОбъектОбработки.ЗаполнитьДеревоСтруктурыХраненияБД(ВременноеДерево, Параметры.АдресВХранилищеСтруктураХраненияБазыДанных );
	
	ЗначениеВРеквизитФормы(ВременноеДерево,"ДеревоСтруктурыХраненияБазыДанных");
	
	ВременноеДерево = Неопределено;
	
КонецПроцедуры
