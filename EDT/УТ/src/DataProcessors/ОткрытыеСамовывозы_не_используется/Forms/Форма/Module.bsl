  &НаСервере
  Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	  Об = РеквизитФормыВЗначение("Объект");
	  Рез = Об.ОбновитьДанные();
	  Выб = Рез.Выбрать();
	  Пока Выб.Следующий() Цикл
		  Нов = Таб.Добавить();
		  Нов.Самовывоз = Выб.Самовывоз;
	  КонецЦикла;	  
  КонецПроцедуры
