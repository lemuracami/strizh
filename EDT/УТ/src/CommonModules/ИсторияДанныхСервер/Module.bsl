Процедура РегламентОбновлениеИсторииДанных() Экспорт
	ЕстьОшибка = Ложь;
	//lem.ОтправитьНормальноеСообщениеАдминам("История данных - начало", "История данных - начало " + Формат(ТекущаяДата(), "ДЛФ=DT"));
	УчетКонтроляВремениСервер.ЗаписатьСобытиеОбновлениеИсторииДанных(ТекущаяДата(), Ложь);
	Попытка
		ИсторияДанных.ОбновитьИсторию();
	Исключение
		ЕстьОшибка = Истина;
		lem.ОтправитьНормальноеСообщениеАдминам("История данных - ошибка", ОписаниеОшибки());
	КонецПопытки;	
	
	Если Не ЕстьОшибка Тогда
		//lem.ОтправитьНормальноеСообщениеАдминам("История данных - окончание", "История данных - окончание " + Формат(ТекущаяДата(), "ДЛФ=DT"));
		УчетКонтроляВремениСервер.ЗаписатьСобытиеОбновлениеИсторииДанных(ТекущаяДата(), Истина);
	КонецеСли;	
КонецПроцедуры	