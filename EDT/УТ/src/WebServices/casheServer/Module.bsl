
Функция getCashier(idKKT)
	UIDD = Новый УникальныйИдентификатор(idKKT);
	ККМ = Справочники.КассыККМ.ПолучитьСсылку(UIDD);
	
	Если Не ЗначениеЗаполнено(ККМ.Код) Тогда
		Возврат "";
	КонецеСли;
	
	Кассир = УчетКассировСервер.ПолучитьКассира(ККМ.Владелец, ККМ, Истина);
	Возврат Строка(Кассир.УникальныйИдентификатор());
КонецФункции
