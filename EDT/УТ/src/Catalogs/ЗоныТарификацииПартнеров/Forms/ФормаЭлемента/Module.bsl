
&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	
	Если Не Объект.НомерЗоны Тогда
		Отказ = Истина;
		ПоказатьПредупреждение(, "Номер зоны не заполнен!");
		Возврат;
	КонецЕсли;
	
КонецПроцедуры
