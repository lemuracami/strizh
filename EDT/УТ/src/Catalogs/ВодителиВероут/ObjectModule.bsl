
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;	
		
	Если ЭтоНовый() Тогда 
		
		УИД = Новый УникальныйИдентификатор;
		
		НовыйВодителиВероутСсылка = Справочники.ВодителиВероут.ПолучитьСсылку(УИД);
   
		ЭтотОбъект.УстановитьСсылкуНового(НовыйВодителиВероутСсылка);
		
		ЭтотОбъект.globalId = Строка(УИД);
		
	КонецЕсли;	
	
КонецПроцедуры
