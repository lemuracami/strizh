
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
		ИП = "Владелец";
		ПЗ = Объект.Ссылка;
		Исп = Истина;
		ОбщегоНазначенияКлиентСервер83.УстановитьЭлементОтбора(ВесовыеБрейки.Отбор,
								ИП,
								ПЗ,
								,
								,
								Исп,
								,
								);
		ОбщегоНазначенияКлиентСервер83.УстановитьЭлементОтбора(ОбъемныеБрейки.Отбор,
								ИП,
								ПЗ,
								,
								,
								Исп,
								,
								);
		Если Не РольДоступна("Админы") Тогда						
			ТолькоПросмотр = Истина;						
		КонецЕСли;	
КонецПроцедуры
