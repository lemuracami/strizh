
// Формирует наименование из реквизитов.
//
Процедура СформироватьНаименование() Экспорт
	Если ЗначениеЗаполнено(Куда) Тогда
		Наименование = СокрЛП(Откуда) + " - " + СокрЛП(Куда);
	Иначе
		Наименование = СокрЛП(Откуда);
	КонецЕсли;
КонецПроцедуры