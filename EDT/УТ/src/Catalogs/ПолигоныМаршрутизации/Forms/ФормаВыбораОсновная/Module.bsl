
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("СхемаПолигонов") Тогда
		СхемаПолигонов = Параметры.СхемаПолигонов;
	КонецЕсли;
	
	// НИКОГДА НЕ РАБОТАЛО, т.к. вместо терминала передавалась дата
	//Если Не ЗначениеЗаполнено(СхемаПолигонов) Тогда
	//	СхемаПолигонов = ПолигональнаяМаршрутизацияСервер.АктивнаяСхемаПолигональнойМаршрутизации(ТекущаяДата());
	//КонецеСли;	
	//
	
	Если ЗначениеЗаполнено(СхемаПолигонов) Тогда
		
		Список.Параметры.УстановитьЗначениеПараметра("СхемаПолигональнойМаршрутизации", СхемаПолигонов);
		Список.Параметры.УстановитьЗначениеПараметра("ПоСхеме", Истина);
		
	Иначе
		
		Список.Параметры.УстановитьЗначениеПараметра("СхемаПолигональнойМаршрутизации", Неопределено);
		Список.Параметры.УстановитьЗначениеПараметра("ПоСхеме", Ложь);
	
	КонецЕсли;

КонецПроцедуры

