#Область ОбработчикиСобытий
&НаКлиенте
Асинх Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	//TODO: Вставить содержимое обработчика.
	//ПараметрыФормы = Новый Структура("", );
	//ОткрытьФорму("Справочник.Заказы.ФормаСписка", ПараметрыФормы, ПараметрыВыполненияКоманды.Источник, ПараметрыВыполненияКоманды.Уникальность, ПараметрыВыполненияКоманды.Окно, ПараметрыВыполненияКоманды.НавигационнаяСсылка);
		
		
	Если Не ЗначениеЗаполнено(ПараметрКоманды) Тогда
		Возврат;
	КонецЕсли;	
		
	//ФормаВызова = ПараметрыВыполненияКоманды.Источник;
	//ВыбранныйРейс = ФормаВызова.ВыбранныйРейс;
	ВыбранныйРейс = ПараметрКоманды;
	
	Если ВыбранныйРейс.Пустая() Тогда
		Возврат;
	КонецЕсли;
	ДанныеРейса = МаршрутизацияСервер.ПолучитьДанныеРейса(ВыбранныйРейс);
	ТипФинальнойТочки = ДанныеРейса.ТипФинальнойТочкиМаршрута;
	БезусловныйЗапросВариантаВозвращения = Ложь;
	Если ТипФинальнойТочки <> 0 Тогда
		ОтветПользователя = Ждать ВопросАсинх("Вы хотите изменить точку возвращения?", РежимДиалогаВопрос.ДаНет);
		Если ОтветПользователя = КодВозвратаДиалога.Да Тогда
			БезусловныйЗапросВариантаВозвращения = Истина;
		КонецЕсли;
	КонецЕСли;
	//TODO: убрать передачу контекста формы, возможно, сделать параметр Рейс
	РезультатОбработки = Ждать МаршрутизацияКлиент.НачатьСборДанныхДляМаршрутизацииЯМАсинх(ВыбранныйРейс, ДанныеРейса, БезусловныйЗапросВариантаВозвращения);
КонецПроцедуры
#КонецОбласти